# Run artifacts

Los runs vNext aceptados conservan JSON/JSONL bajo `<run_id>/` y usan
`.ai/contracts/v2/`. SQLite, caches y archivos temporales permanecen en
`.ai/local/`.

No versionar prompts completos, secretos, outputs sensibles ni logs extensos.
GitHub comments/checks son proyecciones, no copias canónicas.
